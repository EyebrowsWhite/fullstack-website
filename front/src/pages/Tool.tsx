import React, { FC } from 'react';
import ToDo from '../components/Todo';

const Tool:FC = () => {
    return (
        <div className='tool-page'>
            <ToDo />
        </div>
    );
};

export default Tool;